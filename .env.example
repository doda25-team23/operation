# Docker Compose Environment Configuration
# Copy this file to .env and modify as needed
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker-compose up -d
#
# IMPORTANT: Images are private. Before running docker-compose, authenticate:
#   docker login ghcr.io -u <GITHUB_USERNAME>
#   # Enter your Personal Access Token (PAT) with read:packages scope
#
# For Kubernetes, create an image pull secret:
#   kubectl create secret docker-registry ghcr-secret \
#     --docker-server=ghcr.io \
#     --docker-username=<GITHUB_USERNAME> \
#     --docker-password=<GITHUB_PAT> \
#     -n sms-app

# =============================================================================
# Container Images (from GitHub Container Registry - private)
# =============================================================================
FRONTEND_IMAGE=ghcr.io/doda25-team23/app:latest
MODEL_SERVICE_IMAGE=ghcr.io/doda25-team23/model-service:latest

# =============================================================================
# Model Configuration (F10: External model location)
# =============================================================================
# Model version to download from GitHub Releases
# Find available versions at: https://github.com/doda25-team23/model-service/releases
MODEL_VERSION=model-20251121-193920
# Base URL for model artifacts
MODEL_BASE_URL=https://github.com/doda25-team23/model-service/releases/download

# =============================================================================
# Port Configuration (F6: Flexible containers)
# =============================================================================
# Frontend port (inside container)
FRONTEND_PORT=8080
# Frontend port exposed on host (change this if 8080 is already in use)
FRONTEND_HOST_PORT=8080
# Model service port (inside container, not exposed to host)
MODEL_SERVICE_PORT=8081
