{{- if and .Values.monitoring.enabled .Values.modelService.metrics.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "app-stack.fullname" . }}-model-service
  labels:
    {{- include "app-stack.labels" . | nindent 4 }}
    {{- with .Values.monitoring.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: model-service
      {{- include "app-stack.selectorLabels" . | nindent 6 }}
  endpoints:
    - port: http
      path: {{ .Values.modelService.metrics.path }}
      interval: {{ .Values.monitoring.scrapeInterval }}
{{- end }}
